import React, { Fragment } from 'react';

const BrandTitle = ({title}) => {
    return(
        <Fragment>
            <h1 className="brand-title">Quoter</h1>
        </Fragment>
    )
}

export default BrandTitle